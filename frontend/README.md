# Frontend (React + TypeScript)

## Requisitos
- Node.js
- npm en PATH

## Configuracion
1. Copia `.env.example` a `.env`.
2. Ajusta `VITE_API_BASE_URL` si tu backend no corre en `localhost:8080`.

## Ejecutar
```bash
npm install
npm run dev
```
